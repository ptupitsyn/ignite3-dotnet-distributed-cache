# ASP.NET Core Distributed Cache with Apache Ignite 3

1. Start an Ignite cluster with `docker-compose up -d` in `docker` directory.
2. Run the ASP.NET Core app with `dotnet run` in `IgniteDistribCaching` directory.

